{
  "name": "techbridge-official-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check main.js && node --check btx-bot.js && node --check brand-motion.js && node --check worker.js && node --check frontend-worker.js && node --test tests/*.test.mjs",
    "deploy:dry-run": "wrangler deploy --config wrangler.frontend.jsonc --dry-run",
    "deploy": "wrangler deploy --config wrangler.frontend.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
